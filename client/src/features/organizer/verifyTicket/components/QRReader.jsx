const QRReader = () => (
  <>
    <div
      id="qr-reader"
      className="mx-auto w-full max-w-xs rounded-xl overflow-hidden border"
    />
    <p className="text-xs text-gray-500 text-center">
      Align QR code inside the frame
    </p>
  </>
);

export default QRReader;
